package com.elina.school.service;

import com.elina.school.model.Enrollment;

import java.time.LocalDateTime;
import java.util.List;

public interface EnrollmentService {
    /*
    void save(LocalDateTime enroll_date, Long course_id); //course y date
    Enrollment findById(Long enrollment_id);
    List<Enrollment> findAll();
    void addStudents(List<Long> student_ids, Long enrollment_id);
    void addProfessor(Long professor_id, Long enrollment_id);
    void deleteById(Long enrollment_id);
     */

}
